services:
  manga-tracker:
    build: .
    container_name: manga-tracker
    ports:
      - "3003:3003"
    environment:
      - NODE_ENV=production
      - PORT=3003
      - DB_FILE=/data/manga.db
    volumes:
      - manga_data:/data
    restart: unless-stopped

volumes:
  manga_data:
