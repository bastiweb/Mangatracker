services:
  manga-tracker:
    build: .
    container_name: manga-tracker
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DB_FILE=/data/manga.db
    volumes:
      - manga_data:/data
    restart: unless-stopped

volumes:
  manga_data:
